# PartyKit host the browser connects to over WebSocket.
# Local dev (partykit dev) defaults to 127.0.0.1:1999.
# In production set this to your deployed PartyKit host, e.g.
#   sigmaskribbl.<your-partykit-username>.partykit.dev
NEXT_PUBLIC_PARTYKIT_HOST=127.0.0.1:1999
